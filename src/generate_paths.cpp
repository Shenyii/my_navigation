#include "generate_paths.h"

GeneratePaths::GeneratePaths() {
    cout << "generate some paths" << endl;
}

GeneratePaths::~GeneratePaths() {}

vector<Path> GeneratePaths::generatePaths(double start_x, double start_y, double goal_x, double goal_y) {
    path_.clear();
}

vector<Path> GeneratePaths::generatePaths(double start_x, double start_y, double theta, double goal_x, double goal_y) {
    path_.clear();
}